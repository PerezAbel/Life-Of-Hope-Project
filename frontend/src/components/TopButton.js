import React from 'react'; 


const TopButton = () => {
    return (   
        <div className="NavBar">  
        {/* Take user to the top of the page*/  }
        </div>   
    );
};

export default TopButton;
